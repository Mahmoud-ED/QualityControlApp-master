@model Company 
@{
    ViewData["Title"] = ViewContext.RouteData.Values["controller"]?.ToString()
    + " - " + ViewContext.RouteData.Values["action"]?.ToString();
}
<h4>
    <b>@ViewContext.RouteData.Values["controller"]?.ToString()</b>
    - @ViewContext.RouteData.Values["action"]?.ToString()
</h4>
<hr />
<div class="container" style="min-height:71.5vh;">
    <div class="row">
        <div class="col-md-5">
            <div class="alert alert-light" role="alert">
                <b> @Html.DisplayNameFor(model => model.Name):</b>
                @Html.DisplayFor(model => model.Name)
            </div>

            <div class="alert alert-info" role="alert" style="width:max-content">
                <p>
                    <b>@Html.DisplayNameFor(model => model.Created):</b>
                    @Html.DisplayFor(model => model.Created)
                </p>
                <p>
                    <b>@Html.DisplayNameFor(model => model.Modified):</b>
                    @Html.DisplayFor(model => model.Modified)
                </p>
            </div>

            <form asp-action="Delete">
                @* <input type="hidden" asp-for="Id" /> *@
               
                @*------زايداااات يا اخوان للشرح فقط--------------------*@
                @* <input type="hidden" asp-for="Name" /> *@
                @* <input type="hidden" name="test" value="asp" /> *@
                @*--------------------------------------------------------*@

                <input type="submit" value="Delete" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this?')" />
               
          @*       <button type="submit" asp-route-id="@Model.Id" value="Delete"
                        class="btn btn-danger" onclick="return confirm('Are you sure you want to delete this?')">
                    Dlete@* <i class="fa fa-trash-alt text-white"></i> *@
                    <i class="bi bi-trash ms-2 fs-3"></i>
                @* </button> *@ 

                <a asp-action="Index" class="btn btn-outline-danger">Back to List</a>
            </form>
        </div>
    </div>
</div>


