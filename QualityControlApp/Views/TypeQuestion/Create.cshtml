@model QuestionType 

@{
    ViewData["Title"] = ViewContext.RouteData.Values["controller"]?.ToString() +
    " - " + ViewContext.RouteData.Values["action"]?.ToString();
}
<h4>
    <b>@ViewContext.RouteData.Values["controller"]?.ToString()</b>
    - @ViewContext.RouteData.Values["action"]?.ToString()
</h4>
<hr />


<div class="container" style="min-height:71.5vh;">
    <div class="row">
        @if (ViewBag.Message != null)
        {
            <h5 class="alert alert-danger">@ViewBag.Message</h5>
        }
        <div class="col-md-4">
            <form asp-action="Create">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="form-group">
                    <label asp-for="TypeName" class="control-label"></label>
                    <input asp-for="TypeName" class="form-control" />
                    <span asp-validation-for="TypeName" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="QuestionCategoryTypeId " class="control-label"></label>
                    <select asp-for="QuestionCategoryTypeId" class="form-control" asp-items="ViewBag.QuestionCategoryType">
                        <option value="" disabled selected>---------Select category---------</option>
                    </select>
                    <span asp-validation-for="QuestionCategoryTypeId" class="text-danger"></span>
                </div>


                <div class="form-group">
                    <input type="submit" value="Create" class="btn btn-primary" />
                    <a asp-action="Index" class="btn btn-outline-primary">Back to List</a>
                </div>
            </form>
            

        </div>
    </div>
</div>


@section Scripts {
    @{
         await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}

