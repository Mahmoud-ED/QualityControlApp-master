@model IEnumerable<QualityControlApp.Models.Entities.FileType>

@{
    ViewBag.Title = "File Types";
}

<h2 class="mb-4">File Types</h2>

<div class="mb-3">
    <a asp-action="Create" class="btn btn-primary">Create A new</a>
</div>

<table class="table table-bordered table-striped">
    <thead class="table-light">
        <tr>
            <th>N</th>
            <th>Name</th>
            <th> </th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Any())
        {
            var index = 1;
            foreach (var item in Model)
            {
                <tr>
                    <td>@index</td>
                    <td>@item.TypeName</td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Update</a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger" onclick="return confirm('Delete Type؟');">Delete</a>
                    </td>
                </tr>
                index++;
            }
        }
        else
        {
            <tr>
                <td colspan="3" class="text-center text-muted">No Types.</td>
            </tr>
        }
    </tbody>
</table>
